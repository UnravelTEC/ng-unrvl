import { prefixBaseUrl } from '../support/utilities';

describe('App', () => {
/*  it('The user wants to open the app and sees the title.', () => {
    cy.visit('/');
    cy.contains('h1', 'Welcome to app!');
    cy.url().should('eq', prefixBaseUrl('/'));
  });

  it('The user wants to communicate with the server.', () => {
    cy.visit('/');
    cy.get('h4').should('have.text', 'Rest Message: Hallo ich bims ein Test-Rest-Call!');
  });*/
});
